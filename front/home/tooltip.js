"use strict";

/*require("jquery-ui-tooltip");
const $ = require("jquery");

$(() => {

    $( 'img.result-company-tooltip' ).tooltip({
        position: {
            my: "left-10 bottom-5",
            at: "center top",
            using: function( position, feedback ) {
                $( this ).css( position );
                $( "<div>" )
                    .addClass( "arrow" )
                    .addClass( feedback.vertical )
                    .addClass( feedback.horizontal )
                    .appendTo( this );
            }
        }
    });

});
*/
